export type TaintJobRequest = {
    reason?: string | null;
    /**
     * An optional set of key/value pairs that can be used to provide additional context.
     */
    context?: Record<string, any> | null;
};
